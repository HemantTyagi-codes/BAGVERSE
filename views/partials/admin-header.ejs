<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
    <nav class="w-full px-10 py-4 flex justify-between items-center bg-white border-b border-gray-200 shadow-sm">
        <div class="flex items-center gap-2">
            <span class="text-xl font-black tracking-tight text-gray-900">Scatch</span>
            <span class="text-xs bg-blue-100 text-blue-700 font-bold px-2.5 py-0.5 rounded-full uppercase tracking-wider">Admin</span>
        </div>
        <div class="flex items-center gap-6 text-sm font-semibold text-gray-600">
            <a href="/owners/logout" class="text-red-500 hover:text-red-600 transition">Logout</a>
        </div>
    </nav>